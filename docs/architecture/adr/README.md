# Architecture Decision Records (ADRs)

This directory contains Architecture Decision Records (ADRs). Each ADR captures a single decision,
its context, constraints, and operational implications.

## Index

- ADR-0001: CloudFront CSP for Next.js static export uses hash allow-list
- ADR-0002: Zod v4 validation strategy
- ADR-0003: Vitest unit and integration testing strategy
- ADR-0004: Playwright end-to-end testing strategy
- ADR-0005: Static export constraints
- ADR-0006: Static image optimization for Next.js static export
- ADR-0007: External API boundary for static export (contact form)
- ADR-0008: OIDC bootstrap remains manual (GitHub deploy role not managed by CDK)

## Conventions

- Filenames are `ADR-XXXX-<kebab-title>.md`.
- Each ADR includes a decision framework score (must be **≥ 9.0 / 10.0** for finalized decisions).
