# ===========================================
# LOCAL DEVELOPMENT CONFIGURATION
# ===========================================
# Copy this file to .env.local and fill in your values.
# Never commit .env.local to version control.

# Application URLs (local development)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000/api
CONTACT_EMAIL=test@example.com

# AWS Configuration
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=        # Optional: Only needed if not using AWS CLI profile
# AWS_SECRET_ACCESS_KEY=    # Optional: Only needed if not using AWS CLI profile

# ===========================================
# DEPLOYMENT CONFIGURATION (CI/CD only)
# ===========================================
# These are set in GitHub Secrets/Variables, not locally.
# See docs/deployment/environments.md for details.
#
# GitHub Secrets:
#   AWS_DEPLOY_ROLE_ARN=arn:aws:iam::ACCOUNT:role/prod-portfolio-deploy
#   OPENAI_API_KEY=sk-proj-... (for auto-release)
#
# GitHub Variables:
#   NEXT_PUBLIC_BASE_URL=https://your-domain.com
#   NEXT_PUBLIC_API_URL=https://api.your-domain.com
#   NEXT_PUBLIC_APP_URL=https://your-domain.com
#   CONTACT_EMAIL=contact@your-domain.com
#
# AWS SSM Parameter (SecureString):
#   /portfolio/prod/CONTACT_EMAIL = recipient email for contact form
